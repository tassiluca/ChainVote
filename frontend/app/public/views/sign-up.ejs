<!DOCTYPE html>
<html lang="en">    
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Sign up</title>
        <%- include('commons/head') %>
        <link rel="stylesheet" href="../styles/sign.css">
    </head>
    <body>
        <%- include('commons/navbar') %>
        <main>
            <div class="row">
                <div class="col-md-4 center mx-auto">
                    <form id="signUpForm" method="post">
                        <h1 class="h3 text-center mb-3 font-weight-normal">Join our community</h1>
                        <label for="inputName" class="sr-only">Name</label>
                        <input type="text" id="inputName" class="form-control" placeholder="Name" name="name" required autofocus>
                        <label for="inputSurname" class="sr-only">Surname</label>
                        <input type="text" id="inputSurname" class="form-control" placeholder="Surname" name="surname" required autofocus>
                        <label for="inputEmail" class="sr-only">Email address</label>
                        <input type="email" id="inputEmail" class="form-control" placeholder="Email address" name="email" required autofocus>
                        <label for="inputPassword" class="sr-only">Password</label>
                        <input type="password" id="inputPassword" class="form-control" placeholder="Password" name="password" onkeyup="clearPassword(); checkRepeatedPassword();" required>
                        <div id="popup" class="popup hidden">
                            <span class="popuptext" id="popupText">Password does not match rules: Must contain at least one uppercase letter, one lowercase, one number and one special character and its length must be between 8 and 16.</span>
                        </div>
                        <label for="inputRepeatPassword" class="sr-only">Repeat Password</label>
                        <input type="password" id="inputRepeatPassword" class="form-control" placeholder="Repeat Password" onkeyup="checkRepeatedPassword();" required>
                        <label for="inputRole" class="sr-only">Repeat Password</label>
                        <select id="inputRole"  name="role" class="custom-select">
                            <option selected value="user">User</option>
                            <option value="admin">Admin</option>
                        </select>
                        <button id="buttonSignUp" class="btn btn-lg btn-primary btn-block" type="submit">Sign up</button>
                        <div class="text-center mx-auto credentials-route">
                            <p>
                                Already subscribed? <a class="navbar-brand" href="/sign-in">Log in here</a>
                            </p>
                        </div>
                        <div id="error" class="alert alert-danger hidden" role="alert"></div>
                        <div id="success" class="alert alert-success hidden" role="alert"></div>
                    </form>
                </div>
            </div>            
        </main>
        <%- include('commons/footer') %>
        <script src="../js/sign-up.js"></script>
    </body>
</html>